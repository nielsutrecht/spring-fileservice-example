spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:files;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;
    username: sa
    password: pw
  servlet:
    multipart:
      enabled: false

save-dir: ./target/files